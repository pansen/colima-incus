/.zed
.env
/etc/keys/*

# Everything under var/ is machine-local runtime or generated state — nothing
# there is source: the agent-token SECRET, the active-slot/machine-ip pointers,
# the deploy-generated pgdevd.env / pgdevd.service, exports (*.tar.gz), and logs.
/var/
